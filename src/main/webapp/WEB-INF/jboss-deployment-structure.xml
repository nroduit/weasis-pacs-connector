<jboss-deployment-structure>
	<deployment>
		<!-- Exclusions allow you to prevent the server from automatically adding some dependencies <exclusions> <module name="org.slf4j" 
			/> <module name="org.slf4j.impl" /> </exclusions> -->
			
		<!-- exclude-subsystem prevents a subsystems deployment unit processors running on a deployment -->
		<!-- which gives basically the same effect as removing the subsystem, but it only affects single deployment -->
		<exclude-subsystems>
			<subsystem name="logging" />
		</exclude-subsystems>
		<dependencies>
			<module name="org.weasis.weasis-pacs-connector" />
		</dependencies>
	</deployment>
</jboss-deployment-structure>
